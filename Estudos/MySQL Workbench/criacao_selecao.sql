-- Nota: este arquivo original foi mantido como backup. Prefira o script
-- 'criacao_banco_e_lista.sql' que contém comentários linha-a-linha.

-- Cria o banco de dados 'clinica_estetica'. Em ambientes reais, usar
-- 'CREATE DATABASE IF NOT EXISTS clinica_estetica;' para evitar erro se
-- ele já existir.
CREATE DATABASE clinica_estetica;

-- Define o banco de dados atual para execução das próximas instruções.
USE clinica_estetica;

-- Lista os bancos de dados existentes no servidor MySQL.
SHOW DATABASES;

